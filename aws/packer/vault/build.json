{
  "min_packer_version": "0.12.0",
  "variables": {
    "aws_region": "us-west-2",
    "vault_module_version": "v0.11.4",
    "vault_version": "1.0.3",
    "consul_module_version": "v0.4.2",
    "consul_version": "1.3.1"
  },
  "builders": [
    {
      "ami_name": "pbar-vault-{{user `vault_version`}}-amazon-linux-{{isotime | clean_ami_name}}-{{uuid}}",
      "ami_description": "An Amazon Linux AMI that has Vault and Consul installed.",
      "iam_instance_profile": "packer-role",
      "instance_type": "t2.micro",
      "name": "amazon-linux-ami",
      "region": "{{user `aws_region`}}",
      "type": "amazon-ebs",
      "source_ami_filter": {
        "filters": {
          "virtualization-type": "hvm",
          "architecture": "x86_64",
          "name": "*amzn-ami-hvm-*",
          "block-device-mapping.volume-type": "gp2",
          "root-device-type": "ebs"
        },
        "owners": ["amazon"],
        "most_recent": true
      },
      "ssh_username": "ec2-user"
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "inline": ["sudo yum install -y git"]
    },
    {
      "type": "shell",
      "inline": [
        "git clone --branch {{user `vault_module_version`}} https://github.com/hashicorp/terraform-aws-vault.git /tmp/terraform-aws-vault",
        "/tmp/terraform-aws-vault/modules/install-vault/install-vault --version {{user `vault_version`}}"
      ],
      "pause_before": "30s"
    },
    {
      "type": "shell",
      "inline": [
        "git clone --branch {{user `consul_module_version`}} https://github.com/hashicorp/terraform-aws-consul.git /tmp/terraform-aws-consul",
        "/tmp/terraform-aws-consul/modules/install-consul/install-consul --version {{user `consul_version`}};",
        "/tmp/terraform-aws-consul/modules/install-dnsmasq/install-dnsmasq"
      ]
    }
  ]
}
